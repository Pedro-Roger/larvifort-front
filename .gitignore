# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# .env* é ignorado globalmente pelo .gitignore da raiz do projeto, mas
# .env.local.example não tem segredo nenhum — é o template que documenta as
# vars necessárias (VITE_API_URL). Precisa ficar versionado. Mesmo padrão
# aplicado em backend/.gitignore.
!.env.local.example
